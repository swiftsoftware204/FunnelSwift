# Auto-deploy happens server-side via post-merge git hook
# See /opt/swift/funnelswift/deploy-from-vps.sh on the VPS
# This workflow is disabled — VPS_SSH_KEY secret not configured

name: Deploy funnelswift

on:
  push:
    branches: [ main ]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy is handled server-side via post-merge git hook"
